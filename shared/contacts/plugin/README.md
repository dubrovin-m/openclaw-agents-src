# OpenClaw Contacts plugin

Bounded model-visible access to the shared deterministic Contact Registry. It exposes explicit identity operations only and delegates persistence to `contactctl`; no generic SQL, shell, messaging, imports, stakeholder state, or scheduler authority is included.
